{
  "name": "frontend",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "prisma generate && vite build"
  },
  "dependencies": {
    "@prisma/client": "^4.0.0"
  },
  "devDependencies": {
    "prisma": "^4.0.0",
    "vite": "^4.0.0"
  },
  "prisma": {
    "schema": "prisma/schema.prisma"
  }
}
