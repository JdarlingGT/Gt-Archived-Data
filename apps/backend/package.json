{
  "name": "backend",
  "version": "1.0.0",
  "description": "GT Explorer Backend API",
  "main": "main.py",
  "scripts": {
    "start": "python main.py",
    "dev": "nodemon --exec python main.py",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}